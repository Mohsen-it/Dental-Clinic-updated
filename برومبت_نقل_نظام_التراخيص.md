# برومبت لتطبيق نظام التراخيص في مشروع Electron جديد

## السياق

لديك نظام تراخيص كامل من مشروع Dental Clinic موجود على المسار:


المطلوب: نقل نفس نظام التراخيص وتطبيقه في مشروع Electron جديد.

---
#الملفات التي تم اضافتها
### 1. مجلد electron/
نسخ كل الملفات التالية:
```
electron/
├── licenseManager.js          # أو licenseManager.ts
├── licenseManager.ts          # (إن كان موجوداً)
├── deviceBoundLicenseGenerator.js
├── productionLicenseValidator.js
├── usedLicensesTracker.js
├── predefinedLicenses.js
├── preload.js                 # أو preload.ts
└── main.js                    # أو main.ts
```

### 2. مجلد scripts/
```
scripts/
├── generateProductionLicenses.js
├── generateDeviceBoundLicenses.js
└── licenseGenerator.js
```

### 3. ملفات React
```
src/
├── hooks/
│   └── useLicense.ts          # نقله وتخصيص اسم التطبيق
└── components/
    └── auth/
        └── LicenseEntryScreen.tsx  # نقله وتخصيص التصميم
```

### 4. ملف package.json
```
package.json
```
(لنسخdependences فقط: `node-machine-id`, `electron-store`)

---

## الخطوات التفصيلية للـ Agent

### الخطوة 1: تثبيت التبعيات
```bash
npm install node-machine-id electron-store
```

### الخطوة 2: إنشاء مجلد electron في المشروع الجديد
```bash
mkdir -p electron
```

### الخطوة 3: نسخ ملفات الترخيص
```bash
# نسخ من Dental-Clinic-new إلى المشروع الجديد
cp -r /c/Users/OMAR/Desktop/Dental-Clinic-new/electron/* ./electron/
```

### الخطوة 4: إنشاء مجلد scripts
```bash
mkdir -p scripts
cp -r /c/Users/OMAR/Desktop/Dental-Clinic-new/scripts/*.js ./scripts/
```

### الخطوة 5: تحديث preload.js/main.js

**هام جداً:** إضافة هذا الكود في بداية ملف `main.js` أو `main.ts`:

```javascript
// ========== بداية ملف main.js ==========

// Import license manager and predefined licenses
let licenseManager = null
let predefinedLicenses = null
try {
  const { licenseManager: lm } = require('./licenseManager.js')
  licenseManager = lm

  predefinedLicenses = require('./predefinedLicenses.js')
  console.log('✅ License manager loaded successfully')
  console.log('✅ Predefined licenses loaded successfully')
} catch (error) {
  console.error('❌ Failed to load license manager:', error)
}

// ========== IPC Handlers للترخيص ==========
// ضع هذا قبل ipcMain.handle('db:Patients')

// License IPC Handlers
ipcMain.handle('license:checkStatus', async () => {
  try {
    if (!licenseManager) {
      return {
        isValid: false,
        error: 'License manager not available',
        isFirstRun: true
      }
    }

    const validation = await licenseManager.validateStoredLicense()
    const isFirstRun = licenseManager.isFirstRun()

    return {
      isValid: validation.isValid,
      error: validation.error,
      isFirstRun: isFirstRun,
      licenseData: validation.licenseData
    }
  } catch (error) {
    console.error('❌ Error checking license status:', error)
    return {
      isValid: false,
      error: 'License status check failed',
      isFirstRun: true
    }
  }
})

ipcMain.handle('license:activate', async (_, licenseKey) => {
  try {
    if (!licenseManager) {
      return {
        success: false,
        error: 'License manager not available'
      }
    }

    const result = await licenseManager.activateLicense(licenseKey)

    if (result.isValid) {
      return { success: true, licenseData: result.licenseData }
    } else {
      return { success: false, error: result.error }
    }
  } catch (error) {
    console.error('❌ Error activating license:', error)
    return { success: false, error: 'License activation failed' }
  }
})

ipcMain.handle('license:getMachineInfo', async () => {
  try {
    if (!licenseManager) {
      return {
        hwid: 'unavailable',
        error: 'License manager not available'
      }
    }

    const hwid = licenseManager.getCurrentHWID()

    return {
      hwid: hwid,
      platform: process.platform,
      arch: process.arch
    }
  } catch (error) {
    console.error('❌ Error getting machine info:', error)
    return {
      hwid: 'error',
      error: 'Failed to get machine info'
    }
  }
})

ipcMain.handle('license:getLicenseInfo', async () => {
  try {
    if (!licenseManager) {
      return { activated: false, error: 'License manager not available' }
    }

    const info = await licenseManager.getLicenseInfo()
    return info
  } catch (error) {
    console.error('❌ Error getting license info:', error)
    return { activated: false, error: 'Failed to get license info' }
  }
})

ipcMain.handle('license:clearData', async () => {
  const isDev = process.env.NODE_ENV === 'development' || !app.isPackaged
  
  if (!isDev) {
    return { success: false, error: 'License clearing only allowed in development mode' }
  }

  await licenseManager.clearLicenseData()
  return { success: true }
})

ipcMain.handle('license:getStorageInfo', async () => {
  try {
    const info = licenseManager.getStorageInfo()
    return info
  } catch (error) {
    return { error: 'Failed to get storage info' }
  }
})

// ========== نهاية قسم الترخيص ==========
```

### الخطوة 6: تحديث preload.js

إضافة هذا في `preload.js`:

```javascript
// License operations - أضفه في الكائن electronAPI
const electronAPI = {
  // ... باقي الـ API ...

  license: {
    checkStatus: () => ipcRenderer.invoke('license:checkStatus'),
    activate: (licenseKey) => ipcRenderer.invoke('license:activate', licenseKey),
    getMachineInfo: () => ipcRenderer.invoke('license:getMachineInfo'),
    getLicenseInfo: () => ipcRenderer.invoke('license:getLicenseInfo'),
    clearData: () => ipcRenderer.invoke('license:clearData'),
    getStorageInfo: () => ipcRenderer.invoke('license:getStorageInfo')
  }
}

contextBridge.exposeInMainWorld('electronAPI', electronAPI)

// Type declaration - أضفه في declare global
declare global {
  interface Window {
    electronAPI: {
      license: {
        checkStatus: () => Promise<any>
        activate: (licenseKey: string) => Promise<any>
        getMachineInfo: () => Promise<any>
        getLicenseInfo: () => Promise<any>
        clearData: () => Promise<any>
        getStorageInfo: () => Promise<any>
      }
      // ... باقي الـ API
    }
  }
}
```

### الخطوة 7: إنشاء useLicense.ts في src/hooks/

```bash
mkdir -p src/hooks
cp /c/Users/OMAR/Desktop/Dental-Clinic-new/src/hooks/useLicense.ts ./src/hooks/
```

**هام:** إذا كان اسم التطبيق مختلفاً، يجب تغيير:
- `APP_SALT` في `licenseManager.js` إلى اسم التطبيق الجديد

### الخطوة 8: إنشاء LicenseEntryScreen.tsx

```bash
mkdir -p src/components/auth
cp /c/Users/OMAR/Desktop/Dental-Clinic-new/src/components/auth/LicenseEntryScreen.tsx ./src/components/auth/
```

**هام:** تخصيص:
- اسم التطبيق في العناوين
- معلومات التواصل (WhatsApp, Email)
- ألوان الثيم إذا كان مختلفاً

### الخطوة 9: دمج الترخيص في App.tsx

في `App.tsx` أو نقطة الدخول الرئيسية:

```typescript
// الاستيرادات
import { useLicense } from './hooks/useLicense'
import LicenseEntryScreen from './components/auth/LicenseEntryScreen'

function App() {
  const {
    isLicenseValid,
    isFirstRun,
    isLoading: licenseLoading,
    error: licenseError,
    machineInfo,
    activateLicense
  } = useLicense()

  // تحميل شاشة الترخيص إذا غير مفعل
  if (!isLicenseValid || isFirstRun) {
    return (
      <LicenseEntryScreen
        onActivate={handleLicenseActivation}
        isLoading={licenseLoading}
        machineInfo={machineInfo || undefined}
      />
    )
  }

  // تحميل التطبيق الرئيسي بعد التحقق
  return <MainApplication />
}

async function handleLicenseActivation(licenseKey: string) {
  const result = await activateLicense(licenseKey)
  if (result.success) {
    console.log('✅ License activated successfully')
  } else {
    console.log('❌ License activation failed:', result.error)
  }
  return result
}
```

### الخطوة 10: توليد مفاتيح الإنتاج

```bash
cd scripts
node generateProductionLicenses.js 100
```

هذا سينشئ ملف `production-licenses.json` مع 100 مفتاح.

---

## التخصيصات المطلوبة حسب التطبيق

### 1. تغيير APP_SALT في licenseManager.js
```javascript
// قبل النقل
const APP_SALT = 'dental-clinic-license-salt-2025'

// بعد النقل (تغييره لاسم التطبيق الجديد)
const APP_SALT = 'my-app-license-salt-2025'
```

### 2. تغيير اسم ملف electron-store
```javascript
// في licenseManager.js
const licenseStore = new Store({
  name: 'my-app-license-data',  // تغيير الاسم
  encryptionKey: 'my-app-license-encryption-key-2025',  // تغيير المفتاح
  cwd: app.getPath('userData')
})
```

### 3. تغيير MASTER_KEY في deviceBoundLicenseGenerator.js
```javascript
// قبل النقل
this.masterKey = 'DENTAL_CLINIC_MASTER_KEY_2025_SECURE_ENCRYPTION'

// بعد النقل
this.masterKey = 'MY_APP_MASTER_KEY_2025_SECURE_ENCRYPTION'
```

---

## التحقق من نجاح النقل

1. ✅ تشغيل التطبيق يجب أن يظهر شاشة الترخيص
2. ✅ يمكن إدخال مفتاح من predefinedLicenses
3. ✅ بعد التفعيل يحفظ في electron-store
4. ✅ عند إعادة التشغيل يتعرف على الترخيص
5. ✅ معرف الجهاز (HWID) يظهر بشكل صحيح
6. ✅ في وضع التطوير يمكن مسح بيانات الترخيص

---

## الأوامر الكاملة للنسخ

```bash
# إذا كان المشروع الجديد في: C:/Projects/MyNewApp

# 1. نسخ ملفات electron
cp "C:/Users/OMAR/Desktop/Dental-Clinic-new/electron/licenseManager.js" ./electron/
cp "C:/Users/OMAR/Desktop/Dental-Clinic-new/electron/deviceBoundLicenseGenerator.js" ./electron/
cp "C:/Users/OMAR/Desktop/Dental-Clinic-new/electron/productionLicenseValidator.js" ./electron/
cp "C:/Users/OMAR/Desktop/Dental-Clinic-new/electron/usedLicensesTracker.js" ./electron/
cp "C:/Users/OMAR/Desktop/Dental-Clinic-new/electron/predefinedLicenses.js" ./electron/
cp "C:/Users/OMAR/Desktop/Dental-Clinic-new/electron/preload.js" ./electron/

# 2. نسخ scripts
cp "C:/Users/OMAR/Desktop/Dental-Clinic-new/scripts/generateProductionLicenses.js" ./scripts/
cp "C:/Users/OMAR/Desktop/Dental-Clinic-new/scripts/generateDeviceBoundLicenses.js" ./scripts/
cp "C:/Users/OMAR/Desktop/Dental-Clinic-new/scripts/licenseGenerator.js" ./scripts/

# 3. نسخ React hooks و components
mkdir -p src/hooks
mkdir -p src/components/auth
cp "C:/Users/OMAR/Desktop/Dental-Clinic-new/src/hooks/useLicense.ts" ./src/hooks/
cp "C:/Users/OMAR/Desktop/Dental-Clinic-new/src/components/auth/LicenseEntryScreen.tsx" ./src/components/auth/
```

---

## المشاكل الشائعة وحلولها

| المشكلة | الحل |
|---------|------|
| License API not available | تأكد من إضافة handlers في main.js وفتحتها في preload.js |
| HWID غير صحيح | تحقق من تثبيت `node-machine-id` |
| التفعيل يفشل | تحقق من APP_SALT أنه نفسه في جميع الملفات |
| البيانات لا تحفظ | تحقق من `electron-store` مثبتة وصحيحة |
| شاشة الترخيص لا تظهر | تأكد من التحقق من `!isLicenseValid` قبل تحميل التطبيق |

---

## اختصار: سكريبت Bash للنقل التلقائي

```bash
#!/bin/bash
# اسم المشروع الجديد
NEW_PROJECT="MyNewApp"

# نسخ الملفات
cp electron/licenseManager.js "$NEW_PROJECT/electron/"
cp electron/deviceBoundLicenseGenerator.js "$NEW_PROJECT/electron/"
cp electron/productionLicenseValidator.js "$NEW_PROJECT/electron/"
cp electron/usedLicensesTracker.js "$NEW_PROJECT/electron/"
cp electron/predefinedLicenses.js "$NEW_PROJECT/electron/"
cp electron/preload.js "$NEW_PROJECT/electron/"

cp scripts/generateProductionLicenses.js "$NEW_PROJECT/scripts/"
cp scripts/generateDeviceBoundLicenses.js "$NEW_PROJECT/scripts/"
cp scripts/licenseGenerator.js "$NEW_PROJECT/scripts/"

mkdir -p "$NEW_PROJECT/src/hooks"
mkdir -p "$NEW_PROJECT/src/components/auth"
cp src/hooks/useLicense.ts "$NEW_PROJECT/src/hooks/"
cp src/components/auth/LicenseEntryScreen.tsx "$NEW_PROJECT/src/components/auth/"

echo "✅ تم نسخ جميع ملفات الترخيص"
```

---

هذا البرومبت يحتوي على كل ما يحتاجه المطور أو الـ Agent لتطبيق نظام التراخيص في أي مشروع Electron جديد.
